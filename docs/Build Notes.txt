++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++

Build Notes journal
By Oliver McKinlay

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Phase 1 - Getting a working data model

09/09
Just generated the first header files
Now will compile and test the item class and structs to validate.
Refer to step 3 - Compile and test Item.h in ChaptGPT logs.

19/09
Done step 3, the compilation was successful and an item was created. 
There was an issue with the compilation and the header files, because CMake
doesn't have explicit access to the files/folder paths I had to update the CMakelists.txt 
to hvae target_include_directories. Likely becuase of the current file layout, but it works.

Step 7 also now implamented and tested.
The task creation system works as expected for this preliminary test. Currently enum class
only returns integers, the variable names are just nice to work with essentially.
Can implament a function toString() later that prints the correct item to the console
Like this:
        std::string toString(ItemType item)
        {
            switch (item)
            {
                case ItemType::Wallet: return "Wallet";
                case ItemType::Keys: return "Keys";
                case ItemType::Phone: return "Phone";
                case ItemType::Laptop: return "Laptop";
                case ItemType::Charger: return "Charger";
                default: return "Unknown";
            }
        }

Phase 1 Finshed:
Working prototype with custom data types for items, tasks and events.
The data can be changed without affecting the structure of the underlying code.
We now have wokring: 
    enum class ItemType
    struct Item contains all the relavent information about an Item
    Arrays of items and tasks
    Fixed width integers to save space using #include <cstdint>
    Header files with all relavent information that can be passed between builds and .cpp files


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Phase 2 - Implamenting the Inventory Manager
This is basically a mini database:

RFID       Type          Location
--------------------------------------
0x1234     Wallet        Outside
0x5678     Keys          Backpack
0xABCD     Phone         Outside

It has associated functions to query the inventory for specific items or status.

Currently working on Inventory functionality. Generated the Invenotry.h file
This declares all of the functions and private data in the iventory. 
The Inventory.cpp file then implaments all of the functions used by inventory
I have also added a .getItemCount which was not in the ChaptGPT logs but is a useful feature.
Add new function methodology
 1. Define a prototype function in the Inventory.h header file
 2. Implament the function in Inventory.cpp
 3. Call and use the function in Main.cpp
 4. Build and link the files to an executable.

Finshed Phase 3 and moved onto phase 4, refer to dev logs

Left off on Phase 4D the checkInventory function of the chatGPT logs. 
Added another safety check to the TaskManager::startTask function.
Currently working through the BackPack.cpp and Backpack.h files and functions.
This is the FSM implamentation.
Current states include:
    IDLE
    CHECKING_INVENTORY
    ACQUIRING_ITEMS
    TASK_REMINDER


20/09 
Continued work on phase 4.
Have now implamented the following flow chart using FSM

                 startTask()
                     │
                     ▼
                   IDLE
                     │
                     ▼
             TaskManager starts
                  the task
                     │
                     ▼
           CHECKING_INVENTORY
                     │
                     ▼
             checkInventory()
                /          \
               /            \
          complete?          no
             /                \
            ▼                  ▼
    TASK_REMINDER       ACQUIRING_ITEMS


Item tracking flow in the backpack subsystem of phase 4

             RFID subsystem
                    │
                    │ scan
                    ▼
            itemScanned(tag)
                    │
                    │ determine context
             ┌──────┴──────┐
             ▼             ▼
        itemAdded()   itemRemoved()
             │             │
             ▼             ▼
        Inventory     Inventory

Combining this into the FSM we have:

                         startTask()
                              │
                              ▼
                         ┌─────────┐
                         │ CHECKING│
                         │INVENTORY│
                         └────┬────┘
                              │
                    checkInventory()
                         /         \
                        /           \
               complete             missing
                  │                    │
                  ▼                    ▼
          ┌──────────────┐      ┌──────────────┐
          │TASK_REMINDER │      │  ACQUIRING   │
          └──────┬───────┘      │    ITEMS     │
                 │              └──────┬───────┘
                 │                     │
                 │ stopTask()          │ itemAdded()
                 │                     │
                 │                     ▼
                 │              task complete?
                 │                  /     \
                 │                YES      NO
                 │                 │        │
                 │                 ▼        │
                 │          TASK_REMINDER   │
                 │                          │
                 └──────────────────────────┘
                              │
                              │ stopTask()
                              ▼
                         ┌─────────┐
                         │  IDLE   │
                         └─────────┘